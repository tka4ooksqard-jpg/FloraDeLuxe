# Public site origin (canonical, Open Graph, sitemap, robots, JSON-LD).
# Staging / production: set on the host (e.g. https://staging.example.com).
# Local: leave empty to use http://localhost:3000, or set explicitly.
NEXT_PUBLIC_SITE_URL=
